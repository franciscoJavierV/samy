/**
 
just created this repository to allow the dependencies inyection as we spoke earlier but didnot implemented becasue was no part of the main approach 

 * import type { Order } from "../Order.js";
 *
 * export interface OrderRepository {
 *   save(order: Order): Promise<void>;
 *   findById(id: string): Promise<Order | null>;
 * }
 */

export {};
